# Frontend Environment Variables
# This file is committed to git because it only contains non-sensitive defaults

# Backend API URL - defaults to localhost for development
VITE_BACKEND_API_URL=http://localhost:3000

# For production deployment, override this in Vercel Dashboard:
# Settings → Environment Variables → VITE_BACKEND_API_URL
#VITE_BACKEND_API_URL=https://periods-tracker.onrender.com
